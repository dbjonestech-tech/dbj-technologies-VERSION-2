/* ─── CANOPY DEEP DIVES (Layer 3) ──────────────────────
 *
 * Layer 3 of the Canopy work-page funnel. Each entry here renders as a
 * dedicated page at /work/canopy/{slug} and activates the
 * "Read the full architecture of {heading} ->" link inside the matching
 * ProjectSection's Layer 2 toggle on /work/canopy.
 *
 * The body voice extends the Layer 1 + Layer 2 register for the same
 * section: first-person "I", punchy short sentences, declarative,
 * no em dashes, "honest exclusion" pattern, the question-that-drove
 * -the-design rhythm. Each body is roughly 1,500 to 2,000 words and
 * should walk the reader through the question, the rejected
 * alternatives, the load-bearing mechanism, the failure modes guarded
 * against, and the operational consequence.
 *
 * Compliance posture (per .claude/rules/canopy.md): bodies describe
 * Canopy by outcomes and architectural intent, never by internal
 * detail. No lead-score signal enumerations, no audit_log column
 * shape, no Inngest function IDs, no migration numbers, no canopy
 * _settings or pathlight-gate column names, no RBAC enum values, no
 * prompt structure, no per-layer source-rendered detail. The
 * EXISTENCE of the Pathlight three-gate guardrail is explicitly
 * public-OK per canopy.md and is the architectural anchor of the
 * Pathlight Integration page.
 */

const ANALYTICS_BODY =
  "The question I asked first when shaping the analytics surface: what changes when the visitor data and the conversion data live in the same database under the same auth gate.\n\nThe answer turns out to be the kind of question the buyer can ask. With the standard stack, a buyer can ask 'what was my bounce rate last week.' That is what third-party analytics platforms are good at; that is what they sell. A buyer cannot ask 'of the prospects who became deals last quarter, which marketing source did they originate from, and what was their median time from first visit to closed-won.' That second question requires joining two systems that the SaaS contract does not let you join, and the export reconciliation that pretends to support it never quite agrees between tools. Bounce rate is a gauge; the source-to-deal join is a question about whether marketing actually worked. The buyer who is trying to decide where to spend their next dollar needs the second question answered, and the standard stack cannot answer it.\n\nI considered the standard stack: a third-party analytics SDK on the front end, the CRM accessed through its public API, a webhook bridge between the two, an attribution tool layered over the bridge to do the source-to-deal joins, a dashboard layered over the attribution tool to render the joins. Five products in the path, four contracts the buyer is paying for, three integrations that have to stay in sync. I rejected the stack for the same reason I reject most multi-vendor solutions: every layer of it rents access to the buyer's data back to the buyer, and the bridge is what breaks first. When a vendor changes their API, when a pricing tier is sunset, when a product is acquired and the integration goes EOL, the bridge breaks and the buyer's view of their own funnel goes dark until they can rebuild it. The cost compounds across vendors. The data fragments across data centers. The numbers between tools do not agree.\n\nWhat Canopy does instead is structurally simpler and more invasive: the visitor capture writes directly to the buyer's Postgres. The conversion capture writes there too. Every event, including the visitor's first arrival, the page they landed on, the pages they viewed, the click events that crossed the conversion boundary, the form submissions, the scan reports, the deals created from those records, all live in tables in the same database. There is no SDK loaded from a third-party CDN; the capture script is served from the buyer's own domain. There is no third-party tracking cookie; the identifier is a first-party cookie scoped to the buyer's site. There is no event upload to an external data center; the events POST to a route inside the buyer's deployment that writes them straight to the buyer's database.\n\nBot pressure is separated from real visitors at the ingest layer. This is the part most analytics dashboards either over-count, inflating vanity numbers that make the buyer feel good and decisions worse, or under-count, hiding a real signal about scraper attention or scrape-driven cost. Canopy classifies traffic at the moment of capture using a combination of user-agent matching, behavioral fingerprinting, and a watchlist of known automation infrastructure. The classification writes to the same row as the event, so the headline numbers in the dashboard reflect actual potential customers while the bot pressure stays surfaced in the underlying data when the buyer wants to look at it. Both numbers are honest; they just show up in different places. A buyer who is trying to make a marketing decision sees the human-traffic numbers; a buyer who is debugging an unusual cost spike or an unusual bounce rate can drill into the bot view in the same dashboard.\n\nReal-user Web Vitals are captured from the buyer's own site and posted to the same database. Largest Contentful Paint, Interaction to Next Paint, Cumulative Layout Shift, Time to First Byte, First Contentful Paint, all measured on every real visitor session and stored as event rows. The dashboard renders the per-percentile distributions, the per-page breakdowns, and the regression timeline against deploys. When a regression shows up after a deploy, the buyer can answer 'is this real or just bot pressure' against the same database that holds pipeline. They can also answer 'is this regression hurting conversion' in the same dashboard, with the same numbers, against the same auth gate. No browser-tab switching. No quarterly meeting about whose numbers agree.\n\nSearch insight enters the same database through a daily ingest from the buyer's Search Console property. Impressions, clicks, average position, the queries the buyer's site is showing for, all attributed to the pages they landed on. The query data joins to the visitor data on the landing-page key, and the landing-page data joins to the deal data through the standard funnel attribution. The buyer can ask 'which queries produced visitors who became deals' in a single SQL question because all the rows live in the same database. The answer is at the same trust level as the rest of the analytics, which is to say the buyer can audit it, query it, modify it, replicate it.\n\nThe honest exclusion: Canopy is not built for systems with billions of events per day. The architecture assumes the buyer is doing thousands to tens of thousands of events per day, where architectural ownership matters more than horizontal scale. At the very high end of event volume, the right answer is a dedicated event pipeline, a streaming aggregation layer, a separate analytics database optimized for OLAP queries, and a different team to operate it; that is a different product and a different price. The trade-off Canopy is taking is the one that fits a small business with revenue, conversion data, and the appetite to ask precise questions about what is producing the revenue. It is not the trade-off for a high-volume consumer SaaS.\n\nA second honest exclusion: privacy posture is first-party-only by design. There is no third-party tracking cookie, no cross-site identifier, no shared identity graph with other Canopy installs or with the studio. Each install's visitor data stays in that install's database. Raw IP addresses are not persisted; the geolocation derived from the IP at the moment of capture is persisted, but the IP itself is dropped after that derivation. This stance is partly about defensibility under privacy regulation, partly about not surprising the buyer when they look at what is in their database, and partly about the fact that I do not need cross-site identity to answer the questions Canopy is built to answer.\n\nThe dashboard reads from already-aggregated views rather than from the raw event tables on every render. Aggregations refresh on a schedule that fits the freshness requirement of each kind of question: visitor counts every few minutes, Web Vitals percentiles hourly, source-to-deal joins overnight. The dashboard stays fast even as the underlying data grows, because the aggregated views grow much more slowly than the event-row tables do. The buyer's database does not have to be infinitely fast at OLAP queries; the dashboard's read paths are designed around the assumption that the database is normal-good rather than analytical-grade, which is the right assumption for the price tier this is in.\n\nThe operational consequence is the one the architecture targets: when a regression shows up after a deploy, the buyer can answer 'is this real or just bot pressure' against the same database that holds pipeline, in the same dashboard where they review deals, behind the same auth wall. The marketing question, the performance question, and the conversion question are answered by the same numbers in the same place. There is no quarterly conversation about which tool's bounce rate is right because there is only one tool. There is no integration to break because there is no integration. The data is the buyer's, and the questions the data can answer are bounded only by the SQL the buyer is willing to write.";

const PIPELINE_BODY =
  "The question that drives the data model: what does it actually mean to say a CRM is 'the source of truth.'\n\nFor most small-business CRMs, the answer is 'whatever the most recent person to touch the record typed in.' That is not truth; that is the latest belief held by whichever team member happened to be the last edit. Two days later, when the team is trying to remember whether the deal moved to proposal because the prospect's CFO signed off or because nobody updated the record after a stalled call, the latest-belief model produces the wrong answer to the only question that matters: what actually happened. A CRM that cannot tell you what happened, only what was last typed, is a worse-than-useless mirror of the team's own forgetfulness.\n\nCanopy treats truth as the audit log. Every meaningful change to a record writes a before-and-after snapshot attributed to the operator who made the change, with a timestamp and the action or rule that produced it. The current state of any record is the projection of those events, the way you would compute a current account balance from a sequence of bank transactions. The implication is that the database can answer questions the team did not know to ask when they wrote the record, because the events are still there to replay. 'When did this deal first enter the proposal stage' is a query against the audit log, not a guess based on the field's current value. 'Who was the operator on the last three changes to this contact's status' is also a query. The answers are queryable, attributable, and exportable, and they are the same answers two weeks later as they were today.\n\nI considered making contact status the primary axis of the data model, with deals as derived fields on the contact. That is the shape most small-business CRMs use because it is the simpler relational design: each contact has a status field, the status changes over time, and the team's view is contact-shaped. I rejected it because every real business has repeat customers and parallel opportunities. A contact whose first deal closed-won six months ago and whose second deal is currently in proposal cannot be represented as a single status. Collapsing deal state into contact state loses the second engagement, and the second engagement is where most of the revenue lives in a recurring-relationship business. So deals are primary, and contact status is a denormalized mirror of the contact's primary current deal stage, kept for backward compatibility with the old shape, but not the source of truth.\n\nThe mechanism for the deal-stage primary model is straightforward in shape: deals are first-class entities with their own stage transitions, their own probability percentages, their own values, their own timelines. A contact has many deals over time. The pipeline kanban board reads deals, not contacts; the columns are deal stages, the cards are individual deals, and a contact with three open deals shows up as three cards in three columns. The probability rollup is per-deal-stage with per-deal weighting, which is the right unit of forecasting because two deals at twenty-five thousand dollars each in proposal at seventy-five percent probability tells the buyer something the contact-stage model cannot.\n\nThe contact detail view brings together every interaction the buyer has ever had with that person, automatically. Form submissions land as records; email replies are pulled in from the buyer's email account through an authenticated integration; scan reports attach when the contact's domain is scanned; calls logged through the call modal arrive as events; notes added through the contact UI write to the same timeline; deal-stage transitions for any deal owned by the contact also surface there. Nothing has to be manually copied between systems because there are no other systems. The timeline is event-shaped, ordered by time, with each event keeping its provenance so the buyer can ask 'how did this email reach me' and get a real answer instead of 'we synced it from somewhere.'\n\nBulk actions cover the cases where individual record edits do not fit the operator's intent. Move ten deals from contacted to qualified at once. Tag fifty contacts as a Q3 outreach cohort. Reassign a list of contacts to a different operator. Each bulk action writes a single audit-log entry with the affected record IDs and the change applied, so a question like 'who tagged these fifty contacts and when' has a definitive answer. Bulk actions are deliberate, traceable, and reversible by design.\n\nKeyboard navigation works the way an operator who lives in the tool every day expects it to work. The pipeline kanban has keyboard shortcuts for moving deals between stages, navigating between cards, opening the deal detail view, applying bulk selections. The contact list has a command palette that supports fuzzy-search across every contact and deal in the database. The expectation is that an operator should be able to live in Canopy without touching the mouse for an hour at a stretch, the way operators live in their email client and their text editor. Most CRMs do not pay this discipline because their primary market is decision-makers reviewing dashboards rather than operators executing day-to-day work. Canopy's primary market is the operator.\n\nThe pipeline forecasting rolls up at multiple granularities. Per-stage weighted forecast across the whole pipeline; per-operator forecast for a sales team; per-time-window forecast for cohort analysis. The weights are configurable per install because every business's stage probabilities are different. A productized engagement with a long discovery cycle will have proposal probabilities that look nothing like a fast-moving e-commerce product's, and the weights should reflect the buyer's actual close rates rather than a vendor's defaults.\n\nThe audit log is also where regret-protection lives. If an operator marks a deal closed-lost and a week later realizes the prospect actually came back and signed, the audit log holds the previous state and the deal can be restored to its prior position with the audit-log entry recording the restoration. The restoration is itself an event in the audit log, so the question 'what really happened to this deal' produces a complete history rather than a pretendable-clean current state. This is the practical value of event sourcing: not just attribution of changes, but reversibility of changes, and a complete chronicle the buyer can audit.\n\nThe honest framing on what this is not: Canopy's pipeline surface is built for small businesses with under five hundred active deals at a time, where the operators have the time and the discipline to update records. It is not a sales-engagement platform with auto-dialer integration and AI-generated follow-up suggestions; that is a different product for a different scale. The buyer who needs that scale is past the size where Canopy is the right fit, and that is fine; the architecture's honest exclusions are part of the design, not an apology.\n\nThe operational consequence the buyer feels is portability. A team member leaves, the database stays. A new operator joins, the audit log already explains what the previous team did and why. The questions are queryable, the answers are verifiable. The export, if one ever happens, is the buyer's database itself, in its native shape, on infrastructure the buyer pays for directly. The team can leave whenever they like and take the whole pipeline with them, because the pipeline was always theirs.";

const AUTOMATION_BODY =
  "The question that determined the shape of the automation engine: should rules run as side-effects on the writer's path, or as subscribers to a durable event log.\n\nA naive automation engine fires rules inline. The operator marks a deal closed-won, the inline handler runs immediately, the proof-of-craft email goes out, the follow-up task gets created, the audit-log entry is written last. This is the path of fewest moving parts and the lowest latency, and it is also the path where every rule misfire is invisible because the audit log was written after the side-effect, not before. The rule fired, the email sent, the customer received it; the only record of why it fired is in the rule's own configuration at the moment of firing, which the operator may have changed since. Inline automation is fast and brittle.\n\nI chose the second path: every meaningful mutation in Canopy writes to the audit log first. Rules read from the log and act. That is slower by a few hundred milliseconds than firing rules inline; it is also the reason a rule that fires wrong is recoverable. The mutation records the actor and the change. The action records the rule that fired it and the input it was reading. The audit log holds both. So the question 'why did this email send' has a query, not a debugging session against an inline handler's stack trace and a rule configuration that may have changed since.\n\nI considered a no-code automation builder with hundred-step branching lifecycle programs. Drag-and-drop conditions, multi-path branches, A/B-tested templates, the whole pattern. I rejected it for the same reason I reject most product additions: the operator who needs that already bought Hubspot. The operator who has not bought anything yet needs the next ten things to happen without remembering them, not a hundred-step program nobody in the building understands by month four. Lifecycle marketing software is a real product category that deserves to exist; it is just not the product category Canopy is in.\n\nThe mechanism splits into three primitives: sequences, workflows, and rules. Each has a narrow purpose and reads from the audit log in a different way.\n\nSequences are multi-step outbound with reply-exit. A prospect enters a sequence at the operator's invocation; the sequence sends step one immediately or on a delay; step two follows after a configured interval; the prospect can be removed from the sequence by replying to any of the messages, by being moved to a deal stage that exits the sequence, or by the operator manually stopping it. Reply-exit is the part that matters most: a prospect who replies stops receiving the next message in the chain because the audit log writes the inbound reply event, the sequence's exit-condition rule reads that event, and the next step is cancelled. Without reply-exit, automated outbound chains keep firing into a conversation the prospect has already started, and the result is the kind of email cadence that makes recipients hate the sender. Reply-exit is non-optional for the same reason brakes are non-optional on a car.\n\nWorkflows fire on domain events. A new deal hits a stage, an old deal goes silent for an extended window, a scan report flags a finding worth a follow-up, a contact's email bounces three times in a row, a new contact enters the database from a form submission. Each of these is an event in the audit log, and each workflow is a subscription to a particular event shape with a chain of actions to fire when that shape matches. Workflows are expressive: a single workflow can fire multiple actions, condition on multiple fields, and chain into other workflows through the events it itself writes. They are also bounded: the framework limits the depth of workflow chaining so a misconfigured workflow cannot recursively trigger itself.\n\nRules are the lowest-level primitive: they condition on what changed in the audit log, not just what was triggered. The difference matters. A workflow fires on 'a deal was moved to proposal'; a rule fires on 'a deal was moved from qualified to proposal' (the from-state matters), or 'a deal was moved to proposal by operator X' (the actor matters), or 'a deal was moved to proposal AND its value is above twenty-five thousand dollars' (the field condition matters). Rules read the before-and-after snapshot from the audit log entry, not just the entry's event type, which means rules can be specific about what triggered them in ways workflows cannot. The cost of this expressiveness is that rules are a lower-level surface and require more thought to write correctly; the benefit is that the same expressiveness covers cases that would otherwise need custom code.\n\nEmail templates are versioned. A template is a piece of marketing copy with merge fields and an associated subject line, and every template change writes a new version with a timestamp and an actor. When a sequence sends a step using a template, the audit log records which version of the template was sent. If a template is later edited and the operator wants to know 'what did this prospect actually receive last Tuesday,' the answer is queryable: the audit log holds the version sent, and the version is retrievable from the template version history. Templates can also be rolled back: the operator picks a previous version and promotes it to current, which is a separate audit-log event. The send history is preserved across rollbacks, so the buyer can audit what actually went out without reconstructing it from email-platform logs.\n\nBulk actions cover the cases where automation is overkill. The operator wants to send a one-time outbound to a list of contacts, not enroll them in a sequence; the bulk-action tooling lets them compose the message, select the contacts, preview the merged copy, and send. Each bulk action writes a single audit-log entry with the affected contact IDs, the message content, and the actor. The audit log lets the buyer answer 'who sent this email to these fifty contacts' without sifting through fifty individual send records.\n\nEvery automated action writes the rule that fired it back to the audit log. This is the load-bearing inversion of the inline-automation path. When something automates that should not have, the operator opens the audit log, finds the action, reads the rule that fired it, and either tunes the rule or disables it. The trail does not require a debugging session against a vendor's internal logs because the trail is in the buyer's own database. The same query that explains why an email sent also explains why a follow-up task got created or why a workflow chained into another workflow.\n\nThe honest exclusion: this is not a marketing automation suite for thousand-step lifecycle programs. The framework is bounded by the kinds of operations a small-team operator needs to express: 'whenever X happens, do Y and Z, with reply-exit on outbound.' It is not a tool for orchestrating a Fortune-500 customer-lifecycle program with cohort analysis, predictive scoring, and seventeen-step branching journeys. The architecture intentionally stops short of that scale because the operators it is built for would not know how to operate the larger surface, and the operators who do know how to operate the larger surface are not buying small-business engagements.\n\nThe operational consequence the buyer feels is the one the architecture targets: when something automates that should not have, the answer is one query away. The rule that fired is named in the audit log entry. The before-and-after state is retrievable. The rollback is a single bulk action against the affected records. The next ten things happen without the operator remembering them, and when one of those ten was wrong, the system explains itself instead of obscuring the cause. That is the version of automation that fits the architecture, and it is the one that holds up over months of operation.";

const OPERATIONS_BODY =
  "The question that shaped the operations surface: what does an operator actually need to see, and what is just dashboard porn.\n\nMost operations dashboards try to show everything. Five graphs across the top, twelve gauges down the side, a dozen widgets below that, all updating in real time, all colored to attract attention. The operator either trains themselves to ignore the page or they panic-respond to whichever gauge happens to be yellow at the moment they happen to look. Neither outcome ships meaningful work. Both outcomes look responsible to a stakeholder who does not have to live with the dashboard, and that is the only audience the everything-dashboard is actually built for.\n\nI built Canopy's operations surface around the inverse discipline: stay quiet until something matters. The default state is one banner at the top of the dashboard that summarizes the worst signal across the entire stack. Green means everything I bother to monitor is fine. Amber means something needs attention this week. Red means something needs attention right now. The operator visits the dashboard for the work they came to do, not because the operations page demanded their attention, and the only time the operations surface speaks up is when there is something the operator actually has to act on.\n\nThe decision to make the banner the surface and the dashboard the drill-down was deliberate. A buyer who sees the banner amber in the morning has one question, which is 'what do I have to do about it,' and the drill-down has to answer that question in one click. The banner is therefore not a status bar; it is a router. Click the banner and the relevant subsystem opens with the underlying data and the recommended action. Per-domain TLS expiry, WHOIS expiration, email-authentication posture, deployment health, deliverability rollups, function-level error volume, real-time spend against the configured budget ceilings, all behind the same banner-shaped entry point.\n\nI considered always-on real-time alerting that paged the operator on every threshold crossing. I rejected it for the reason most alerting layers eventually fail: most small businesses get three alerts and then mute the channel, and a muted channel is worse than no channel because it manufactures false confidence. The buyer thinks they are covered because the alerting tool is wired up; the alerting tool is firing into a channel nobody reads. The right design is the inverse: the operations surface speaks rarely and authoritatively, and the operator trusts that silence means OK. Trust requires that silence be earned through low false-positive rates and high true-positive recall, and the architecture is tuned for both.\n\nDaily cron is enough for everything I check at the operations layer where the underlying signal moves slowly. Certificate expiry moves on a 90-day cycle for the kinds of certificates Canopy installs use, which means a daily check has eighty-nine days of warning before any cert can fail. WHOIS expiration moves on a similar timeline. Email authentication posture moves only when DNS records change, which is rare. The daily cron writes its findings to the audit log at the same time it updates the banner, so the trail is durable and the question 'when did this start failing' is answerable from the audit log without consulting the cron's own logs.\n\nReal-time is reserved for the signals that genuinely change minute-to-minute. Deployment health is one: a deploy that fails has to surface in the operations banner immediately, not eight hours later when the next daily cron runs. Email deliverability is another: a sender domain whose authentication has been flagged by a major provider has to show up in the banner before the next campaign goes out. Function-level error volume is the third: a runtime error that started ten minutes ago and is still firing has to be visible in real time so the operator can decide whether to roll back. These signals get ingested as they happen, written to the same database the banner reads from, and the banner reflects them on the next page render.\n\nThe infrastructure-health checks themselves are run daily per tracked domain. The buyer can configure which domains they want monitored, and each check is an independent query against the relevant external service. TLS validity is a probe against the domain's port 443 with certificate inspection. WHOIS expiration is a query against the domain registrar's WHOIS service. SPF validity is a DNS lookup with a rule check; same for DKIM and DMARC. Each check returns a status and a description, both of which write to the audit log alongside the check's timestamp. If a check fails, the banner reflects the failure on the next render. If a check succeeds for the first time after a previous failure, the banner clears and the audit log holds the clear event for posterity.\n\nDeliverability ingests from the buyer's transactional email provider through an authenticated integration. The ingest captures send events, delivery events, bounce events, and complaint events, and the operations surface aggregates these into the banner-level signal that the buyer's outbound is healthy. A spike in bounces relative to the rolling baseline triggers an amber banner; a spike in complaints triggers red. The operator can drill into the deliverability page from the banner and see the per-recipient detail, the affected sender domain, and the recommended action.\n\nFunction-level error volume comes from the runtime's error reporting integration. Canopy is deployed to a runtime that captures unhandled errors and reports them with stack traces and request context. The operations layer ingests these reports, dedupes them by error signature, and surfaces the top errors in the dashboard. A new error type that has fired in the last hour is visible in the banner as amber; a sustained error that is affecting many requests is red. The drill-down from the banner shows the per-error detail, the affected paths, and the link to the runtime's own error tracker for deeper investigation.\n\nBudget headroom is monitored in real time against the buyer's configured monthly cap for AI integration spend. The cap is the same one the Pathlight integration enforces atomically, and the operations banner reads the current period's usage and displays the percentage remaining. When the usage crosses eighty percent of the cap, the banner reflects amber; at ninety-five percent, red. The drill-down shows the per-action history of what spent the budget and the period reset date. The operator can also adjust the cap from the same surface, with the change writing to the audit log.\n\nThe failure mode I built against most carefully is the one most operations surfaces miss: a TLS certificate expiring at two in the morning. The operator is asleep. The certificate stops being valid at the moment of expiration. Every visitor to the site between then and the operator waking up sees a certificate error and bounces. Even if the certificate is renewed within an hour of the operator waking up, the marketing damage is done. The right defense is not to alert at the moment of expiration; it is to surface the upcoming expiration days before. Canopy's daily cron flags any certificate within thirty days of expiry as amber and any within seven days as red. The buyer is warned during working hours, when warnings can become tickets instead of incidents.\n\nThe honest exclusion: this operations surface is not a substitute for a dedicated infrastructure-monitoring tool at scale. A buyer running a high-traffic consumer site with many points of failure across many providers needs a more sophisticated monitoring stack with alerting fan-out, on-call rotation, and incident runbooks. Canopy's operations layer is built for small businesses where the operator is also the on-call engineer, where the failure modes are well-bounded, and where the discipline of staying quiet is more valuable than the depth of an alerting matrix. The architecture's exclusions are deliberate.\n\nThe operational consequence the buyer feels is trust. Operations is not a dashboard you visit in panic when something has broken; it is a banner you trust because it stays quiet when it should and speaks up only when you have to act. The drill-down is there when you need it; the banner does the daily work.";

const PATHLIGHT_BODY =
  "The question that gates this section: how do you let an operator press a button that costs the studio real money, without exposing the buyer to a runaway bill.\n\nThat sentence sounds like a side-effect of caring about cost, but it is actually the load-bearing constraint that shaped the entire integration. Anywhere AI is in the path, cost is a wild-card unless something deliberately stops it from being one. The standard answer is 'usage-based billing with a soft alert threshold.' I have read enough postmortems on that pattern to know what happens when the alert fires at three in the morning UTC and the engineer who can flip the switch is asleep: the bill keeps growing for the entire window between the alert and the response. Every alerting layer I have ever shipped on top of usage-based billing has eventually leaked through. So I built the integration on the inverse posture: the bill cannot grow past the cap, by construction, regardless of whether anyone is awake.\n\nThe integration ships behind three independent gates. Each gate is capable of stopping the call on its own. All three have to pass for the call to fire. Capability has to be turned on per install. The trigger has to be manual or rule-bounded with explicit human approval, never a silent background scheduler. The monthly budget cap has to have headroom, checked and reserved atomically against the very same write that records the spend. If any one gate fails the call aborts and the operator sees the gate that blocked it. The buyer never gets a surprise bill from a runaway loop because the architecture does not allow runaway loops.\n\nI considered scheduled background scans with usage-based billing as the obvious starting point. Wake up, scan the prospect list, surface findings to the operator. It is the pattern most agency tools ship. I rejected it for two reasons. The first is the cost-runaway problem above: a bug in the scheduler that re-enqueues tasks faster than they complete cannot be bounded by a soft threshold; only a hard cap with atomic reservation can stop it. The second is the operator-trust problem: when the system can scan on its own, the operator is no longer the agent who chose to spend the studio's money. Every Canopy install has to feel like a shop the buyer owns the keys to, and a system that bills the buyer for actions the buyer never authorized is the opposite of that.\n\nThe mechanism for the budget cap is the part that took the most iteration. The naive shape is a check-then-reserve: read the current period's usage, compare it to the cap, if there is headroom, fire the call, then write the new usage row. Two concurrent admin clicks at the same instant when the budget has one scan of headroom remaining can both pass this check, both fire, and the buyer ends up over the cap. That is a textbook time-of-check-vs-time-of-use bug, and the textbook fix is atomic check-and-reserve: the same write that records the new usage row is conditional on the cap not being exceeded. Either the row lands and the call is allowed to fire, or the row does not land and the call gets a user-facing block. There is no window where two concurrent paths can both pass. I shipped that fix in the May 5 audit closure work; before that, the gate was correct in the single-operator case but had this concurrency window. The fix is a single conditional database write; either it affects one row or zero rows, never two.\n\nThe capability toggle and the manual-trigger requirement are simpler in mechanism and just as important in posture. Capability is per-install: a Canopy install where the buyer has not opted into Pathlight integration cannot fire any call no matter how many other gates would pass. This means the integration ships dark on every new install and lights up only when the buyer signs the engagement that includes it. The manual-trigger requirement means there is no cron, no scheduler, no event-loop feedback that can fire a Pathlight call without an operator having clicked something. Rule-bounded triggers exist, where a deal moves to a stage where rescanning makes sense or a customer site flags a change worth checking, but every rule that fires Pathlight requires explicit human approval before the call goes through. The rule queues the call as awaiting approval; the operator approves; the call fires. Approval is not a checkbox on the rule itself; it is a per-call action.\n\nThe integration surfaces in three places inside Canopy. Prospecting candidate research is the most common: an operator selects a contact or a list of contacts, kicks off a Pathlight scan against each prospect's domain, and the scan output flows into the pipeline as a record attached to the contact. The scan score becomes a sortable field; the recommendations attach to the prospect's notes; the operator can compose a follow-up email referencing specific findings. Change monitoring is the second: existing customer sites get rescanned on a schedule, but the rescans are queued for operator review rather than auto-fired, and the queue surfaces in the operations banner when something is waiting. Competitive intelligence is the third: an operator can scan a buyer's direct competitor and store the result alongside the buyer's own scan history, so the next conversation about positioning has data on both sides of the table. All three flow through the same three-gate path. There is no privileged code path that bypasses the gates for any reason.\n\nThe failure mode I most carefully guarded against is the one that originally motivated the atomic check-and-reserve: two admin clicks at the same instant when the budget has one scan of headroom remaining. Both have to not pass. One scan fires, the other gets a user-facing block that names the budget cap as the reason. The block is a real UI surface, not a thrown error: the operator sees 'budget cap reached, this scan was not run' and a link to the operations banner where the cap and the period reset date are visible. The same shape applies if the capability toggle is off, or if the manual approval is missing for a rule-fired trigger. The architecture's job is to make the block legible, not just to prevent the call.\n\nA second failure mode worth naming: the gate fires, the call attempt happens, and the upstream Pathlight service errors out for an unrelated reason. The reservation has been written and the budget has been decremented; the call did not produce a result. The right behavior is a refund: a release that adds the reserved units back to the available budget. Canopy implements that release in the failure path so the buyer is never charged for a call that did not produce a deliverable. The release is atomic against the same row the reservation wrote, for the same reason the reservation has to be atomic in the first place.\n\nThe honest framing on cost: AI as a recurring line item belongs in a productized engagement only when the cost is a known monthly maximum, not a wild-card. Canopy's pricing for installs that include Pathlight integration is structured around the budget cap. The buyer chooses a cap that fits the volume of scans they expect to run; the studio runs scans against that cap; the cap is the ceiling. There is no scenario where the buyer's bill exceeds the cap because there is no code path that allows a scan to fire when the cap has been reached. Every line item is bounded.\n\nThe operational consequence the buyer feels is exactly the one the architecture targets: the AI line item is a fixed monthly maximum, every month, by construction. Not by hope, not by alerting, not by post-hoc reconciliation. The cap is the ceiling, the cap is enforced at the same write that records the spend, and the buyer never gets a surprise bill from a runaway loop. That is the version of AI integration that belongs in a productized engagement, and as far as I can tell, it is the only version that does.";

const ARCHITECTURE_BODY =
  "The question that shaped every other answer: who owns the data after the engagement ends.\n\nMost small-business software answers that question with the phrase 'we do.' The buyer rents access to their own data and pays a monthly subscription for the privilege. The data lives in the vendor's infrastructure, behind the vendor's auth, governed by the vendor's terms of service, and the buyer's leverage is limited to the threat of cancellation, which on a 90-day terms-of-service notice period is often slower than the next quarter's planning cycle. The architecture I built for Canopy inverts that. The buyer owns the data, the buyer owns the infrastructure, the buyer owns the auth, and the buyer can fire me on any day they choose and keep operating tomorrow morning. That is the test that ownership actually has to pass, and it shaped every other technical decision in the stack.\n\nThe mechanism is per-install everything. Each Canopy install is its own Postgres database, its own deployment to a Vercel project the buyer owns, its own Google sign-in OAuth client wired to an admin-only allow-list of email addresses the buyer controls, its own domain registered to the buyer's account, its own SSL certificate issued to that domain, its own monitoring infrastructure, its own audit log. There is no shared infrastructure between customers. There is no multi-tenant database. There is no central admin dashboard where the studio can switch between installs by clicking a customer in a list. The studio gets access to a buyer's install only because the buyer has explicitly added the studio's email to that install's admin allow-list, and the buyer can revoke that access on any day with a single configuration change.\n\nI considered multi-tenant SaaS with row-level security and a master billing dashboard, which is the standard pattern and the one that is cheaper for me to operate. The infrastructure is one database, one deployment, one auth wall, one billing system; new customers are rows in tables instead of new infrastructure provisioning. Most agency-built dashboards ship this way because the alternative is more expensive to run. I rejected it for the buyer's worst case. Every multi-tenant system I have ever read about has eventually leaked across tenants, either through a row-level security misconfiguration, a query that forgot the tenant filter, an admin who clicked through the wrong customer, or a provider-level breach that affected the shared infrastructure. The buyer who matters in the rejection decision is the one who reads the postmortem and remembers it, not the one whose worst day was a slow login. If the worst case for the buyer is a row of their data appearing in someone else's account, that is too bad a worst case to design toward, and the cost saving on operations is not worth the cost cost.\n\nThe per-install architecture has consequences I have to absorb on the studio side. Provisioning a new install is more work than adding a row to a table; it involves creating a new Vercel project, a new Postgres database, a new Google Cloud OAuth client, configuring DNS, issuing the SSL certificate, running the schema migrations against the empty database, configuring the per-install settings, and onboarding the buyer to the admin sign-in. That is a few hours of work per install instead of a few seconds. The compounding cost of this choice is real and I chose it deliberately because the alternative cost falls on the buyer, and the buyer is the one paying.\n\nOperating consequences also flow from the per-install pattern. Software updates have to be deployed to each install separately because the deployments are separate. There is no central rollout. Schema migrations have to be applied to each Postgres database. Security patches that affect the auth layer have to be rolled out per install. I built tooling that automates these per-install operations from a single command, so the operational tax is bounded, but the tax is real and the buyer is the one who benefits from it: the buyer's install does not get migrated to a new region without their permission, does not get a database schema change they did not approve, does not get a feature flag flipped by someone else's customer support ticket.\n\nRole-based access control gates who can see what, with multiple permission tiers configurable per install. Each install's admin allow-list controls who can sign in at all. Inside the admin surface, role assignments determine who can see which sections, who can edit which records, who can change settings, who can manage other operators' access. The role assignments are per install, not shared across installs, and the buyer's primary admin can revoke any role at any time. Every meaningful change to a record is attributable to a specific operator via the audit log, and every change to a role assignment is itself a record in the audit log. The whole architecture is designed around the principle that the buyer should be able to answer 'who did what' at any point in their install's history.\n\nThe audit log itself is the system of record for change attribution and is treated as immutable in normal operation. Every meaningful entity change writes a before-and-after snapshot attributed to the operator who made the change, with a timestamp and the rule or action that triggered the change. The audit log is queryable by the buyer's primary admin without any vendor mediation. If the buyer ever needs to investigate a question like 'who marked this deal closed-lost on this date and what was the previous value', the answer is one query away. If the buyer ever needs to investigate a security question like 'did anyone outside our operator team access this contact's record', the answer is also one query away. This is what attributability means in practice: not just that the system records who did what, but that the buyer can ask the question without me being in the loop.\n\nThe portability story matters too. Because the install is the buyer's deployment, the buyer's database, the buyer's auth, and the buyer's domain, the buyer can take the whole stack with them on any day they choose. There is no export-to-CSV pattern that hides the structure of the data behind a denormalized flat-file. The data is already in the buyer's database in its native shape; the buyer can run any SQL they want against it; the buyer can replicate it to another database; the buyer can hire a different developer to extend it. The integrations the install talks to are configured with the buyer's API credentials, not the studio's; the buyer can rotate the credentials and the studio loses access to those integrations. The deployment is the buyer's Vercel project; the buyer can change the deployment hooks to point at a different repository if they want to fork the codebase.\n\nThe firing test is the cleanest way I have found to articulate what ownership has to mean. If the buyer wakes up one morning and decides they no longer want to work with the studio, they should be able to keep operating tomorrow morning. The data is still there. The auth still works. The domain still resolves. The deployment still serves traffic. The audit log is still queryable. The role assignments are still in force. Nothing breaks because the studio's role in the install was always advisory and configurational, never load-bearing on the runtime. The buyer can hand the install to a different developer for ongoing maintenance, can extend the codebase without my involvement, can run the install for years without ever talking to me again. That is the test that ownership has to pass, and the per-install architecture is the version of the system that passes it.\n\nThe operational consequence the buyer feels is the one that quietly pervades every other section of this case study: a Canopy install is a piece of infrastructure the buyer fully owns, not a SaaS subscription with a long export procedure. The dashboards, the data, the authentication, the domain, the audit trail, and the deployment all live where the buyer keeps the rest of their business. The studio's job is to build the install correctly and hand it over; the buyer's job is to operate it. That handoff is the moment ownership becomes real, and the architecture is what makes the handoff a clean line instead of a renewable contract.";

export interface CanopyDeepDive {
  /** URL slug under /work/canopy/. Kebab-case, no leading slash. */
  slug: string;
  /** Path to the OG / Twitter card image, relative to /public. Reuses
   * the same capability screenshot the matching ProjectSection uses on
   * /work/canopy so the social-share preview reads as a continuous
   * surface with the parent case study. */
  image: string;
  /** Alt text for the OG image. Should describe the screenshot content,
   * not the page topic, since the alt is read when the image is the
   * subject (e.g. social-card simulation, screen reader narration). */
  imageAlt: string;
  /** Page heading. Mirrors the matching ProjectSection heading on
   * /work/canopy so the buyer's "Read the full architecture of X" click
   * lands on a page titled X. */
  heading: string;
  /** Two-or-three sentence summary used as the page's hero subheading
   * and as the meta description. Should orient the reader to what they
   * will read at depth. */
  summary: string;
  /** Long-form architectural narrative. Roughly 1,500 to 2,000 words.
   * Uses blank-line paragraph breaks; the layout renders the string with
   * whitespace-pre-line so source blank lines become real paragraph
   * spacing. */
  body: string;
}

export const CANOPY_DEEP_DIVES: CanopyDeepDive[] = [
  {
    slug: "analytics",
    heading: "Analytics & Performance",
    summary:
      "First-party visitor data, real-user Web Vitals, and search insight all captured directly into the buyer's Postgres, joined to the conversion data without an integration bridge. What the standard analytics stack cannot answer, the architectural cost of owning the data instead of renting access to it, and the kind of question this design lets a buyer ask.",
    image: "/images/case-studies/canopy/canopy-analytics.webp",
    imageAlt:
      "Canopy Analytics and Performance dashboard showing first-party visitor counts, real-user Web Vitals, top pages, and top sources",
    body: ANALYTICS_BODY,
  },
  {
    slug: "pipeline",
    heading: "Pipeline & Relationships",
    summary:
      "Deal-stage primary, the audit log as the system of record, contact timeline auto-aggregated across every interaction the buyer has ever had with that person. Why the source-of-truth question shapes everything, why I rejected contact-status-as-primary, and what the data lets a team do that a typical CRM does not.",
    image: "/images/case-studies/canopy/canopy-deals.webp",
    imageAlt:
      "Canopy Deals kanban board with weighted pipeline, unweighted pipeline, closed-won, and average cycle metrics across New, Contacted, Qualified, Proposal, Won, and Lost columns",
    body: PIPELINE_BODY,
  },
  {
    slug: "automation",
    heading: "Automation",
    summary:
      "Sequences with reply-exit, workflows on domain events, rules conditioned on the audit log. Why automation reads from the event log rather than firing as side-effects on the writer's path, what that costs in latency, and what it gives back in recoverability.",
    image: "/images/case-studies/canopy/canopy-automation.webp",
    imageAlt:
      "Canopy Sequences and Workflows view listing active sequences, enrolled and replied counts, and rule-based workflow triggers",
    body: AUTOMATION_BODY,
  },
  {
    slug: "operations",
    heading: "Operations & Health",
    summary:
      "One banner that summarizes the worst signal across the whole stack, daily cron checks for the slow-moving infrastructure questions, real-time ingest for the fast-moving deliverability and error signals. Why I built around the inverse discipline of staying quiet until something matters.",
    image: "/images/case-studies/canopy/canopy-operations.webp",
    imageAlt:
      "Canopy Operations and Health view showing the All Systems Normal banner across deployments, pipeline, budget, infrastructure, errors, and mobile RUM, plus a per-domain TLS, WHOIS, SPF, DKIM, and DMARC posture table",
    body: OPERATIONS_BODY,
  },
  {
    slug: "pathlight",
    heading: "Pathlight Integration",
    summary:
      "Three independent gates so the studio can run AI-driven research and rescans inside Canopy without ever exposing a buyer to a runaway bill. The architecture, the rejected alternatives, the failure modes I guarded against, and the line item that becomes a fixed monthly maximum by construction.",
    image: "/images/case-studies/canopy/canopy-pathlight.webp",
    imageAlt:
      "Canopy Pathlight Integration view showing layered guardrails for capability, triggers, and monthly budget, plus a prospecting candidates table with scan scores and outreach status",
    body: PATHLIGHT_BODY,
  },
  {
    slug: "architecture",
    heading: "Architecture & Ownership",
    summary:
      "Per-install everything, no shared infrastructure, no multi-tenant database, the buyer pays the vendors directly. Why this is harder for me to operate, what it gives the buyer, and the firing test that ownership has to pass.",
    image: "/images/case-studies/canopy/canopy-audit.webp",
    imageAlt:
      "Canopy Audit log entries showing deal stage changes, contact follow-up updates, email sends, tag edits, and scan completions with key-by-key diffs",
    body: ARCHITECTURE_BODY,
  },
];

export function getCanopyDeepDive(slug: string): CanopyDeepDive | undefined {
  return CANOPY_DEEP_DIVES.find((p) => p.slug === slug);
}

export function getCanopyDeepDiveSlugs(): string[] {
  return CANOPY_DEEP_DIVES.map((p) => p.slug);
}

/* Returns the adjacent pages in registry order so a reader who finishes
 * one deep-dive can chain straight into the next one without bouncing
 * back to the parent case study. Linear, no wraparound: the first page
 * has no prev, the last page has no next. The "Back to Canopy" anchor
 * remains the explicit exit. */
export function getAdjacentCanopyDeepDives(slug: string): {
  prev: CanopyDeepDive | undefined;
  next: CanopyDeepDive | undefined;
} {
  const i = CANOPY_DEEP_DIVES.findIndex((p) => p.slug === slug);
  if (i === -1) return { prev: undefined, next: undefined };
  return {
    prev: i > 0 ? CANOPY_DEEP_DIVES[i - 1] : undefined,
    next: i < CANOPY_DEEP_DIVES.length - 1 ? CANOPY_DEEP_DIVES[i + 1] : undefined,
  };
}
